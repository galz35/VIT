# Credenciales de Prueba Detectadas

El sistema ha detectado los siguientes usuarios existentes en la base de datos local:

| ID | Nombre | Correo | Rol Detectado |
|----|--------|--------|---------------|
| 271 | Ervind Martinez | `ervind.martinez@claro.com.ni` | User |
| - | Pablo Ampie | `pablo.ampie@claro.com.ni` | User |

> **Nota:** Intenta iniciar sesión con la contraseña por defecto: `123456`, `admin123`, o `password`.

## Endpoint de Diagnóstico
Se habilitó temporalmente `/api/debug-users` para obtener esta lista. Se recomienda deshabilitarlo en producción.
